import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Configuración optimizada para Next.js 16 + Turbopack */
  experimental: {
    // Si necesitas configuraciones específicas de Turbo, van aquí
  }
};

export default nextConfig;
